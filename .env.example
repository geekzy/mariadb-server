# Copy this file to `.env` and fill the values. Do NOT commit `.env`.
# Example placeholders for MariaDB container configuration.

# MariaDB root password (used by the container)
MYSQL_ROOT_PASSWORD=passw0rd

# Optional: default database to create on first run
# MARIADB_DATABASE=exampledb

# Allow root to connect from any host (default `%`)
MYSQL_ROOT_HOST=%

# (Git-related placeholders removed on request)
